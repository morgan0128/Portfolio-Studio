import { ReorderGlowDirective } from './reorder-glow-directive';

describe('ReorderGlowDirective', () => {
  it('should create an instance', () => {
    const directive = new ReorderGlowDirective();
    expect(directive).toBeTruthy();
  });
});
